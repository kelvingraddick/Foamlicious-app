import { useColorScheme } from 'react-native';

const useTheme = () => {
  // TODO: drive theme from system and local storage

  // const colorScheme = useColorScheme();
  // return colorScheme;

  return 'dark';
}

export default useTheme;